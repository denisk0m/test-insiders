import React from 'react';
import RegisterForm from "@/ui/components/forms/auth/register-form";

type Props = {
  
}

const RegisterPage: React.FC<Props> = ({  }) => {
  return (
    <RegisterForm/>
  );
};

export default RegisterPage;
