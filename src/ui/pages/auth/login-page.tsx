import React from 'react';
import LoginForm from "@/ui/components/forms/auth/login-form";

type Props = {
  
}

const LoginPage: React.FC<Props> = ({  }) => {
  return (
    <div>
      <LoginForm/>
    </div>
  );
};

export default LoginPage;
